/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    appDir: false, // Keeping pages/ directory enabled
  },
};

module.exports = nextConfig;
